import React from 'react';
import { Flex } from '../dist/index.es';

export default { title: 'Button' };


export const withEmoji = () => (
  <Flex debug spacing={{
    padding: 10,
  }}>

  </Flex>

);